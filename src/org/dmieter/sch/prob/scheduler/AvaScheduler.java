package org.dmieter.sch.prob.scheduler;

import org.dmieter.sch.prob.entity.Job;
import org.dmieter.sch.prob.entity.ResourceDomain;

/**
 *
 * @author dmieter
 */
public class AvaScheduler {
    public boolean schedule(Job job, ResourceDomain domain, SchedulerSettings settings){
        return false;
    }
}
